global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'coffeestudio-backend'
    static_configs:
      - targets: ['host.docker.internal:8000']

# Add additional scrape_configs for exporters (node_exporter, redis_exporter) as needed
